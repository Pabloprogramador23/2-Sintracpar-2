/* Importação da fonte Roboto (Google Fonts) */
@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@600;700&display=swap');

/* Reset de estilos para garantir uniformidade entre navegadores */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Roboto', sans-serif;
    background-color: #e0f2f7;
    color: #263238;
    line-height: 1.6;
    }
    /* Container Principal - controla a largura máxima do conteúdo */
    .container {
    max-width: 1200px; /* Largura máxima do site */
    margin:0 auto; 
    padding: 20px;
    }

    header {
        padding: 40px;
        background-color: #006600; /* Verde escuro do Sindicato */
        color: #fff; /* Texto branco no header */ 
        display: flex; /* Usando Flexbox para organizar os elementos */
        align-items: center;  /* Alinha os elementos verticalmente ao centro */
    }
    
    .container-header {
        display: flex; 
        align-items: center;
        width: 100%; 
        justify-content: center; /* Centraliza horizontalmente */
        flex-direction: column;
      }
    header h1 {
        margin: 0;
        font-size: 40px; 
        margin-left: 40px;
        align-items: center;
    }
    
    header h2 {
        margin: 0; /* Removendo margem padrão */
        font-size: 30px;
        margin-left: 60px;
        align-items: center;
    }

header, main, footer {
    padding: 20px;
}

/* Estilos para a seção de destaque */
destaque {
    background-color: #fff;
    padding: 30px;
    border-radius: 8px;
    margin-bottom: 40px;
    text-align: center;
    }

    .destaque h2 {
    color: #006600;
    font-size: 2.5em;
    margin-bottom: 20px;
    }

    .destaque .verificado {
    display: inline-block;
    padding: 5px 10px;
    background-color: #FFFFE0; /* Amarelo claro */
    color: #006600;
    border-radius: 4px;
    font-weight: bold;
    margin-bottom: 15px;
    }

/* Estilos para as notícias */
.noticias {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* Responsivo */
    grid-gap: 20px;
}

.noticias {
display: grid;
grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); 
grid-gap: 30px;
padding: auto;
}
.noticia {
background-color: #fff;
border-radius: 8px;
box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Sombra suave */
overflow: hidden; /* Para o efeito suave com a imagem */
transition: transform 0.3s ease; /* Efeito de hover - movimento suave */
}
.noticia:hover {
transform: translateY(-5px); /* Move o card levemente para cima */
}
.noticia img {
width: 100%;
height: 200px; /* Altura fixa para as imagens /
object-fit: cover; / Ajusta a imagem ao container */
}
.noticia .conteudo {
padding: 20px;
}
.noticia h3 {
color: #006600;
margin-bottom: 10px;
}
.noticia p {
margin-bottom: 15px;
}
.noticia a.ler-mais {
display: inline-block;
padding: 10px 20px;
background-color: #006600;
color: #fff;
text-decoration: none;
border-radius: 4px;
transition: background-color 0.3s ease;
}
.noticia a.ler-mais:hover {
background-color: #004d00;
}

/* Estilos para os banners laterais */
.banners {
    margin-top: 40px;
    display: grid;  /* Ativamos o Grid Layout */
    grid-template-columns: repeat(3, 1fr); /* 3 colunas com largura igual */
    grid-gap: 20px;  /* Espaçamento entre os banners */
}

.banner {
    background-color: #fff;
    padding: 20px;
    border-radius: 8px;
    text-align: center;
    margin-bottom: 20px; 
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);  /* Sombra suave */
    transition: transform 0.3s ease; 
}

.banner:hover {
    transform: scale(1.05);  /* Aumenta o tamanho levemente */
}

.banner img {
    /* Estilos para a imagem do banner - você irá adicioná-las depois */
    max-width: 100%;  /* Garante que a imagem não ultrapasse o container */
    height: auto;
}

.banner a {
    display: block;
    padding: 10px 20px;
    background-color: #006600;
    color: #FFFFE0;
    text-decoration: none;
    border-radius: 4px;
    transition: background-color 0.3s ease; 
}

.banner a:hover {
    background-color:  #004d00; 
}
/* Estilos para a seção Mais Lidas */
/* ... (seu código CSS anterior) ... */

/* Seção Mais Lidas */
.mais-lidas {
    margin-top: 60px; 
  }
  
  .mais-lidas h2 {
    color: #006600; 
    font-size: 2em;
    margin-bottom: 20px;
  }
  
  .mais-lidas ul {
    list-style: none;  /* Remove os marcadores da lista */
    padding: 0; 
  }
  
  .mais-lidas li {
    display: flex; 
    align-items: center; 
    padding: 15px 0;
    border-bottom: 1px solid #E0E0E0; /* Linha suave como divisória */ 
  }
  
  .mais-lidas img {
    width: 80px; /* Largura da miniatura */
    height: 60px; /* Altura da miniatura */
    object-fit: cover; /* Ajusta a imagem ao tamanho definido */
    border-radius: 4px; /* Cantos arredondados na imagem */
    margin-right: 15px;
  }
  
  .mais-lidas a {
    text-decoration: none; 
    color: #263238;  /* Cor do link - mesma do texto padrão */
    transition: color 0.3s ease; 
  }
  
  .mais-lidas a:hover {
    color: #006600; 
  }
  
  .mais-lidas .numero { 
    font-weight: bold; 
    margin-right: 10px;
  }
  
  .mais-lidas .titulo { 
    color: #FFFFE0; /* Amarelo claro para o título */ 
    font-weight: 600; /* Título um pouco mais espesso */
  }
